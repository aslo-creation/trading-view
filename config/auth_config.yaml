# config/auth_config.yaml
# Passwords are bcrypt hashes generated by make_user.py — NEVER plaintext.
# The cookie key below is a placeholder; inject the real value from the
# COOKIE_SIGNING_KEY environment variable at deploy time (see app bootstrap),
# or rotate this file out of version control entirely.
credentials:
  usernames:
    admin:
      name: admin
      role: admin
      password: $2b$12$/vKlqLed4ll7p2h8eh/nEencvhrspvi0/fK/Nv.Te8ZG4TUR2JFUS
cookie:
  name: quant_terminal_session
  key: "4a099fba2bef2b3fb105aafe6510c4ee4f6dd7afb4f51d181f08711659cef367"
  expiry_days: 1
