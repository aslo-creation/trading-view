streamlit>=1.38
streamlit-authenticator>=0.4.1
bcrypt>=4.1
python-dotenv>=1.0
httpx>=0.27
pandas>=2.2
numpy>=1.26
PyYAML>=6.0
yfinance>=0.2.40     # données de marché en direct (gratuit, sans clé API)
feedparser>=6.0.11   # actualités mondiales via flux RSS (gratuit, sans clé)
plotly>=5.20         # graphiques en bougies interactifs
websockets>=12.0     # flux Binance WSS (TLS) — optionnel
