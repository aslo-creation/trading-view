# Copiez ce fichier vers .env (jamais commité — il est dans .gitignore).
# SEULE clé OBLIGATOIRE — générez-la avec :
#   python -c "import secrets; print(secrets.token_hex(32))"
SESSION_SIGNING_KEY=

# OPTIONNELLES (l'app fonctionne sans, avec des fonctions en moins) :
ANTHROPIC_API_KEY=        # active le débat du Comité IA (sinon : abstention)
FRED_API_KEY=             # séries macro réelles (sinon : séries de démo)
ALPHAVANTAGE_API_KEY=     # source de données alternative
BINANCE_API_KEY=
BINANCE_API_SECRET=
